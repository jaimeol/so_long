La  función checker va a ser únicamente para llamar a las funciones que imprimen el error

Para hacer(Parseo):
-Mirar que el mapa termine en .ber

Para hacer(Juego):
-Buscar la p y guardar la posición para que aparezca cuando se tengan todas las monedas.

